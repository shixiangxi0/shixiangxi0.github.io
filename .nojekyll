
现代前端里，有一个重要特性，组件是独立自治的：持有表现，但不持有逻辑。典型的例子就是：点击按钮自增一

把这个思路再扩展一层：组件可以持有行为，但不能持有逻辑。

举个例子：玩家走进敌人的视线，敌人会做出反应——亮出武器、逼近、摆出攻击姿态。这些全是敌人自己完成的，属于行为。但只要攻击还没真正命中（也就是说玩家的生命值还没扣、游戏状态还没变），这一切就让敌人自治，不需要上报给任何地方。

这就是表现与逻辑的拆分。在这套架构里，表现和逻辑完全独立：

刮风、下雨 → 表现层自己处理
获得装备、扣血 → 上报给逻辑层
逻辑层只干一件事：接收上报的事件，算出新的状态，然后下发一份快照。表现层拿到快照，自己决定怎么呈现——血条怎么滑、屏幕怎么震、角色怎么踉跄。给定同一份快照，表现层产出的动画必须是确定的，不能掺杂真正的随机。

唯一需要区分的就是当前的状态是否会影响游戏未来的状态，会就上报逻辑层，不会救表现层自治

这和 React 的虚拟 DOM 本质上是一回事。React 里，状态变化产生虚拟 DOM，对比差异后批量刷到真实 DOM。游戏架构里，逻辑层产生快照，表现层对比新旧快照后驱动连续动画。

唯一的区别是：虚拟 DOM 改变的是静态 UI，而游戏快照改变的是连续动画。